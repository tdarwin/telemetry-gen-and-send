# Docker Compose Environment Variables
# Copy this file to .env and fill in your values

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# Your Honeycomb API key (required)
# Get this from: https://ui.honeycomb.io/account
HONEYCOMB_API_KEY=your-api-key-here

# ============================================================================
# OPTIONAL CONFIGURATION (with defaults)
# ============================================================================

# Container image settings
# IMAGE_NAME=honeycomb/telemetry-gen-and-send
# VERSION=latest

# OTLP endpoint (default: api.honeycomb.io:443)
# For Honeycomb: api.honeycomb.io:443
# For EU region: api.eu1.honeycomb.io:443
# For local testing: localhost:4317
# OTLP_ENDPOINT=api.honeycomb.io:443

# Sending configuration
# RATE_LIMIT_EPS=100000           # Events per second (spans + metrics + logs)
# CONCURRENCY=30                  # Number of concurrent workers
# DURATION=0                      # Duration to send ("0" = continuous, "5m" = 5 minutes)
# MULTIPLIER=0                    # Template replay count (0 = infinite)

# Batch sizes
# BATCH_SIZE_TRACES=3000          # Traces per batch
# BATCH_SIZE_METRICS=100          # Metric data points per batch
# BATCH_SIZE_LOGS=200             # Log records per batch

# Timestamp configuration
# JITTER_MS=1000                  # Random jitter in milliseconds
# BACKDATE_MS=0                   # Backdate timestamps (0 = current time)

# Connection security
# INSECURE=false                  # Set to true for insecure connections

# ============================================================================
# USAGE EXAMPLES
# ============================================================================

# Continuous operation (default):
#   DURATION=0 MULTIPLIER=0 docker compose up

# Time-limited test (run for 5 minutes):
#   DURATION=5m docker compose up

# High-throughput test (scale senders):
#   docker compose up --scale sender=10

# Custom rate limit:
#   RATE_LIMIT_EPS=500000 CONCURRENCY=50 docker compose up
